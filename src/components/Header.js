import React from 'react';

// Minimal placeholder header to keep the app stable when the visual header is removed.
// Returns null to render nothing, matching the "no header" UI requirement.
const Header = () => null;

export default Header;

